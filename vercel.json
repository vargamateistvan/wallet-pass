{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": null,
  "installCommand": "yarn install",
  "buildCommand": "echo \"Current dir: $(pwd)\" && ls -la && npx turbo build --filter=@wallet-pass/shared && echo \"Changing dir to apps/frontend\" && cd apps/frontend && yarn build:vercel",
  "outputDirectory": "apps/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
