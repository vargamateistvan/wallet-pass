{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "buildCommand": "yarn build",
  "outputDirectory": "apps/frontend/dist",
  "installCommand": "npm install -g corepack && corepack enable && corepack prepare yarn@4.7.0 --activate && yarn install --immutable",
  "devCommand": "yarn workspace frontend dev",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/**/*.ts": {
      "runtime": "nodejs20.x"
    }
  },
  "env": {
    "ENABLE_EXPERIMENTAL_COREPACK": "1"
  }
}
