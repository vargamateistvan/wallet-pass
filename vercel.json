{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": null,
  "installCommand": "yarn install",
  "buildCommand": "echo \"Current dir: $(pwd)\" && npx turbo build --filter=@wallet-pass/shared && yarn workspace frontend build:vercel && mv apps/frontend/dist dist",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
