{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "framework": null,
  "installCommand": "yarn install",
  "buildCommand": "echo \"Debug: Listing directories\" && ls -F && ls -F apps/ && ls -F apps/frontend/ && yarn turbo build:vercel --filter=frontend && echo \"Debug: Finding dist\" && find . -type d -name dist",
  "outputDirectory": "apps/frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
